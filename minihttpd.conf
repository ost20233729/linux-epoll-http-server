# minihttpd 配置文件
# 格式：key value，支持 # 注释；命令行参数优先级高于本文件。
# 运行中修改本文件后，发送 SIGHUP 信号（kill -HUP <pid>）即可热加载。

port 8080
document_root ./www
access_log ./logs/access.log
error_log ./logs/error.log
keepalive_timeout 10
